exports.removeFile = function(req, res, next){
	const fs = require('fs');
	
	var dgram = require('dgram');
	var queryID = Math.floor((Math.random()*100)+1);
	var messageDHARMA = new Buffer("Finished attack: id="+req.query.id+" queryID="+queryID+"");
	var messageMW = new Buffer("DELETE" +req.query.id);
	var client = dgram.createSocket("udp4");
	client.send(message, 0, message.length, 5120, "192.168.10.100", function(err) {
		client.close();
	});
	client.send(message, 0, message.length, 5000, "192.168.10.100", function(err) {
		client.close();
	});
	res.end();


}
